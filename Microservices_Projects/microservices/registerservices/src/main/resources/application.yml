server:
  port: 8999

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false


spring:
  cloud:
    config:
      import-check:
        enabled=false